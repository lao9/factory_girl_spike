<h1><span class="whiteblock"> <%= @category.name %></span></h1><br>

<div class="row">
    <% @category.items.each do |item| %>
      <%= render partial: 'shared/item_card', :locals => {:item => item} %>
    <% end %>
</div>
