<h1>All Pickles</h1>

  <%= button_to "View Cart", carts_path, method: :get %>

<% @items.each do |item| %>
  <h2><%= item.title %></h2>
  <p> <%= item.description %> </p>
  <p> <%= item.price %> </p>
  <img src="<%=item.image_url%>" alt="<%=item.title%>">

  <%= button_to "Add to Cart", carts_path, params: {item_id: item.id} %>
<% end %>
