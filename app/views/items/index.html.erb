<h1><span class="whiteblock">All Pickles</span></h1><br>

<div class="row">
    <% @items.each do |item| %>
      <%= render partial: 'shared/item_card', :locals => {:item => item} %>
    <% end %>
</div>
